// Global feature flags (PRD §10.5 FR-023: AI explanation can be disabled by
// configuration). Flip a flag to show/hide a feature across the app.
export const FEATURES = {
  /** Show the AI Explanation panel on model pages. */
  aiExplanation: false,
};
