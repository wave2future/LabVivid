import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.github.wave2future.labvivid',
  appName: 'LabVivid',
  webDir: 'dist'
};

export default config;
